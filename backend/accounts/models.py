# This app currently relies on Django's built-in User model (see
# django.contrib.auth.models.User) for authentication, so no extra
# models are needed yet. Kept as a dedicated app so a Profile model
# (avatar, bio, favourite genres, etc.) can be added later without
# touching the watchlist app.
